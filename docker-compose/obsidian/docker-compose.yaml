services:

  obsidian:
    container_name: obsidian
    image: ghcr.io/sytone/obsidian-remote:latest
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - UMASK=${UMASK}
      - TZ=${TZ}
    ports:
      - ${PORT_OBSIDIAN}:8080
    volumes:
      - ${CONFIG_OBSIDIAN}:/config
      - ${CONFIG_OBSIDIAN}/vaults:/vaults
      - ${CONFIG_OBSIDIAN}/fonts:/usr/share/fonts/truetype
    restart: unless-stopped